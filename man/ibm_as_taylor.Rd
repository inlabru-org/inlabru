% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mappers.R, R/mapper_collect.R,
%   R/mapper_expr.R, R/mapper_repeat.R, R/mapper_sum.R, R/model.R
\name{ibm_as_taylor}
\alias{ibm_as_taylor}
\alias{ibm_as_taylor.default}
\alias{ibm_as_taylor.bm_multi}
\alias{ibm_as_taylor.bm_collect}
\alias{ibm_as_taylor.bm_expr}
\alias{ibm_as_taylor.bru_obs}
\alias{ibm_as_taylor.bru_obs_list}
\alias{ibm_as_taylor.bm_repeat}
\alias{ibm_as_taylor.bm_sum}
\alias{ibm_as_taylor.bru_comp}
\title{Compute a mapper linearisation}
\usage{
ibm_as_taylor(mapper, input, state = NULL, ...)

\method{ibm_as_taylor}{default}(mapper, input, state, ...)

\method{ibm_as_taylor}{bm_multi}(mapper, input, state, inla_f = FALSE, ...)

\method{ibm_as_taylor}{bm_collect}(mapper, input, state, inla_f = FALSE, ...)

\method{ibm_as_taylor}{bm_expr}(
  mapper,
  input,
  state,
  ...,
  derived = NULL,
  jacobians = NULL,
  data = data,
  inla_f = FALSE
)

\method{ibm_as_taylor}{bru_obs}(mapper, input, state, ..., multi = FALSE, comp_mappers)

\method{ibm_as_taylor}{bru_obs_list}(
  mapper,
  input,
  state,
  ...,
  multi = FALSE,
  comp_mappers,
  eval_fun = NULL
)

\method{ibm_as_taylor}{bm_repeat}(mapper, input, state, ...)

\method{ibm_as_taylor}{bm_sum}(mapper, input, state, ...)

\method{ibm_as_taylor}{bru_comp}(mapper, input, state = NULL, ...)
}
\arguments{
\item{mapper}{A mapper S3 object, inheriting from \code{bru_mapper}.}

\item{input}{Data input for the mapper.}

\item{state}{A vector of latent state values for the mapping,
of length \code{ibm_n(mapper, inla_f = FALSE)}}

\item{...}{Arguments passed on to other methods}

\item{inla_f}{logical; when \code{TRUE} for \code{ibm_n()} and \code{ibm_values()}, the
result must be compatible with the \code{INLA::f(...)} and corresponding
\code{INLA::inla.stack(...)} constructions.  For \verb{ibm_\{eval,jacobian,linear\}},
the \code{input} interpretation may be different.
Implementations do not normally need to do anything different, except
for mappers of the type needed for hidden multicomponent models such
as "bym2", which can be handled by \code{bm_collect}.}

\item{derived}{The state vectors of variables derived from the root
variables. If \code{NULL} or missing, defaults to an empty list.}

\item{jacobians}{The state vectors of variables derived from the root
If \code{jacobians} is \code{NULL} or missing, defaults to an empty list.
If not \code{NULL}, should be a list with named entries, one for variable derived
from the root variables. Each list element should be a named list of Jacobian
matrices, with names matching the root variables.
Missing entries are treated as all-zero matrices.}

\item{data}{should be a list with data objects, with the main object called
\code{data}; see \code{\link[=bm_expr]{bm_expr()}} for details.}

\item{multi}{logical;
If \code{TRUE} (or positive), recurse one level into sub-mappers}

\item{comp_mappers}{A list of mappers, typically from
\verb{as_bm_list<bru_comp_list>}.}

\item{eval_fun}{A list of functions, typically from
\code{\link[=bru_eval_fun]{bru_eval_fun()}}.}
}
\value{
A \link{bm_taylor} object.
The \code{state0} information in the affine mapper indicates for which state
the \code{offset} was evaluated; The affine mapper output is defined as

\if{html}{\out{<div class="sourceCode">}}\preformatted{effect(input, state) =
  offset(input, state0) + jacobian(input, state0) \%*\% (state - state0)
}\if{html}{\out{</div>}}
}
\description{
Implementations must return a \link{bm_taylor} object
The linearisation information includes \code{offset}, \code{jacobian}, and \code{state0}.
The state information indicates for which state the \code{offset} was evaluated,
with \code{NULL} meaning all-zero.
The linearised mapper output is defined as

\if{html}{\out{<div class="sourceCode">}}\preformatted{effect(input, state) =
  offset(input, state0) + jacobian(input, state0) \%*\% (state - state0)
}\if{html}{\out{</div>}}

The default method calls \code{\link[=ibm_eval2]{ibm_eval2()}} to generate
the needed information.
}
\section{Methods (by class)}{
\itemize{
\item \code{ibm_as_taylor(default)}: Calls \code{\link[=ibm_eval2]{ibm_eval2()}} and returns a \link{bm_taylor} object.

}}
\examples{
m <- bm_linear()
ibm_as_taylor(m, input = c(1, 3, 4, 5, 2), state = 2)

}
\seealso{
Other mapper methods:
\code{\link{bru_mapper_generics}},
\code{\link[=ibm_eval]{ibm_eval()}},
\code{\link[=ibm_eval2]{ibm_eval2()}},
\code{\link[=ibm_inla_subset]{ibm_inla_subset()}},
\code{\link[=ibm_invalid_output]{ibm_invalid_output()}},
\code{\link[=ibm_is_linear]{ibm_is_linear()}},
\code{\link[=ibm_is_rowwise]{ibm_is_rowwise()}},
\code{\link[=ibm_jacobian]{ibm_jacobian()}},
\code{\link[=ibm_n]{ibm_n()}},
\code{\link[=ibm_n_output]{ibm_n_output()}},
\code{\link[=ibm_names]{ibm_names()}},
\code{\link[=ibm_simplify]{ibm_simplify()}},
\code{\link[=ibm_values]{ibm_values()}}
}
\concept{mapper methods}
