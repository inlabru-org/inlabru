% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gcpo.R
\name{bru_gcpo_table}
\alias{bru_gcpo_table}
\title{Compare block-averaged GCPO scores across multiple fitted bru models}
\usage{
bru_gcpo_table(fits = NULL, ...)
}
\arguments{
\item{fits}{A named list of fitted \code{bru} objects, or \code{NULL} if models are
passed via \verb{\\dots}.}

\item{\dots}{Named fitted \code{bru} objects, used when \code{fits} is not a list.}
}
\value{
For a single-likelihood model, a \code{data.frame} with columns:
\describe{
\item{\code{block}}{Block labels from \code{response_data$BRU_block} (for \code{"cp"}
models) or observation indices (for other models).}
\item{one column per model}{GCPO scores on the probability scale, named
after the elements of \code{fits} or \verb{\\dots}.}
}
For multi-likelihood models, a named list of such \code{data.frame}s, one per
likelihood.
}
\description{
Calls \code{\link[=bru_block_gcpo]{bru_block_gcpo()}} on each fit and combines the results into a
\code{data.frame} with one row per block and one column per model, making it
straightforward to compare GCPO scores across models block by block.
}
\examples{
\donttest{
if (bru_safe_inla()) {
  cvpart <- cv_hex(
    gorillas_sf$boundary,
    cellsize = 0.5,
    n_group = 3,
    resolution = c(95, 80)
  )
  cvpart$block_ID <- seq_len(nrow(cvpart))
  cvpart$group <- NULL

  nests <- gorillas_sf$nests
  a <- sf::st_intersects(nests, cvpart)
  nests$.block <- unlist(a)

  fit1 <- lgcp(
    geometry ~ Intercept(1),
    data = nests,
    samplers = cvpart,
    domain = list(geometry = gorillas_sf$mesh),
    control.gcpo = list(enable = TRUE, type.cv = "joint")
  )
  pcmatern <- INLA::inla.spde2.pcmatern(
    gorillas_sf$mesh,
    prior.sigma = c(1, 0.01),
    prior.range = c(0.1, 0.01)
  )
  fit2 <- lgcp(
    geometry ~ Intercept(1) + field(geometry, model = pcmatern),
    data = nests,
    samplers = cvpart,
    domain = list(geometry = gorillas_sf$mesh),
    control.gcpo = list(enable = TRUE, type.cv = "joint")
  )
  gcpo_df <- bru_gcpo_table(Model1 = fit1, Model2 = fit2)
  names(gcpo_df)
  colSums(log(gcpo_df[, -1]))
}
}
}
\seealso{
\code{\link[=bru_block_gcpo]{bru_block_gcpo()}}
}
