% Generated by roxygen2 (4.1.1): do not edit by hand
% Please edit documentation in R/pproc_lgcp.R
\name{pproc_lgcp}
\alias{pproc_lgcp}
\title{INLA inference in log Gaussian Cox processes for distance sampling data.}
\usage{
pproc_lgcp(data, int.points = NULL, int.scheme = NULL,
  int.args = list(truncation = max(detdata(data)$distance)),
  dist.trafo = function(dst) {     return(-0.5 * dst^2) },
  covariates = list(), formula = y ~ -1 + Intercept + f(distance, model =
  "clinear", range = c(0, Inf), hyper = list(beta = list(initial = 0, param =
  c(0, 0.1)))) + f(spde, model = spde.mdl), spde.args = list(alpha = 2,
  prior.variance.nominal = 10, theta.prior.prec = 0.01), predict = list(),
  constant = list(), inla.args = list(family = "poisson"), sgh.E = 0,
  int.filter = NULL, det.filter = NULL, sgh.y = 1, int.y = 0,
  stack = list())
}
\arguments{
\item{data}{A distance sampling or SECR data set}

\item{int.points}{Integration points for the approximation of the LGCP denominator}

\item{int.scheme}{Function that maps a data set to integration points. Needed if no integration points are provided.}

\item{int.args}{List of arguments passed on to int.scheme()}

\item{dist.trafo}{Function that transforms distance covariate if integration and observation points. Default: -0.5*d^2}

\item{covariates}{List of functions that add covariates to data frames of integration/observation points}

\item{formula}{The formula that is used in the call to INLA}

\item{spde.args}{List of parameters passed on to inla.spde2.matern() when constructing the SPDE}

\item{predict}{List of linear combinations to predict. Each list entry is a list with two fields. "vars" holds the list of variables to include in the prediction and "loc" describes the locations to predict at.}

\item{constant}{Function providiong constants to add to the linear predictor}

\item{inla.args}{List of arguments passed on to INLA. Default: list(family = "poisson").}

\item{sgh.E}{Poisson likelihood exposure parameter for detections (see INLA documentation)}

\item{int.filter}{Function applied to the data frame of integration points. Use this to filter out particular integration points.}

\item{det.filter}{Function applied to the data frame of detection points. Use this to filter out particular detection points.}

\item{sgh.y}{Observation values for sightings. Default: 1}

\item{int.y}{Observation values for intefration points. Default: 0, later in the alogrithm replaced by integration weights}
}
\value{
\code{pproc_lgcp}, lgcp object.
}
\description{
INLA inference in log Gaussian Cox processes for distance sampling data.
}
\examples{
\\\dontrun{data(whales) ; pp = pproc_lgcp(whales); plot(pp)}
}
\author{
Fabian E. Bachl <\email{f.e.bachl@bath.ac.uk}>
}

