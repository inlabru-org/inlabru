% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{bru_eval}
\alias{bru_eval}
\title{Evaluate or sample from a posterior result given a model and locations}
\usage{
bru_eval(
  model,
  state,
  data = NULL,
  data_extra = NULL,
  input_comp = NULL,
  comp_mappers = NULL,
  predictor = NULL,
  format = NULL,
  used = NULL,
  ...
)
}
\arguments{
\item{model}{A \link{bru} model}

\item{state}{list of state lists, as generated by \code{\link[=bru_state]{bru_state()}}}

\item{data}{A \code{list}, \code{data.frame}, \code{af}, or \code{Spatial*DataFrame}, with
coordinates and covariates needed to evaluate the predictor components.}

\item{data_extra}{Additional data for the predictor evaluation}

\item{input_comp}{Precomputed inputs list for the components}

\item{comp_mappers}{Precomputed \link{bm_list} of full
(\code{as_bm_list(as_bru_comp_list(model))}) , simplified, or linearised mappers
for the components}

\item{predictor}{A formula or a \link{bru_pred_expr} expression to be evaluated
given the posterior or for each sample thereof. The default (\code{NULL})
returns a \code{data.frame} containing the sampled effects. In case of a formula
the right hand side is used for evaluation.}

\item{format}{character; determines the storage format of predictor output.
Available options:
\itemize{
\item \code{"auto"} If the first evaluated result is a vector or single-column matrix,
the "matrix" format is used, otherwise "list".
\item \code{"matrix"} A matrix where each column contains the evaluated predictor
expression for a state.
\item \code{"list"} A list where each element contains the evaluated predictor
expression for a state.
}}

\item{used}{A \code{\link[=bru_used]{bru_used()}} object, or NULL (default)}

\item{\dots}{Additional arguments, unused.}
}
\description{
Evaluate
}
\examples{
if (bru_safe_inla()) {
  model <- bru_model(
    as_bru_comp_list(~ 1 + x + field(idx,
      model = "iid",
      mapper = bm_index(5L)
    )),
    bru_obs(
      y ~ Intercept + x + field,
      family = "poisson",
      data = data.frame(
        y = rpois(5, 10), x = rnorm(5),
        idx = c(1, 2, 3, 4, 5)
      )
    )
  )
  bru_eval(
    model,
    state = list(bru_state(model, property = "zeros")),
    data = data.frame(x = rnorm(4), idx = 4:1)
  )
  bru_eval(
    model,
    state = list(list(
      Intercept = 1, x = 2, field = c(0, 0, 0, 0, 0),
      Precision_for_field = 1
    )),
    data = data.frame(x = rnorm(4), idx = 4:1)
  )
  bru_eval(
    model,
    state = list(list(
      Intercept = 1, x = 2, field = c(0, 0, 0, 0, 0),
      Precision_for_field = 1
    )),
    data = data.frame(x = rnorm(4), idx = 4:1),
    predictor = new_bru_pred_expr(
      ~ list(a = Intercept + x + field, b = field_eval(c(5:7, 5:7)))
    )
  )
}

}
