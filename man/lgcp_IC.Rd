% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deltaIC.R
\name{lgcp_IC}
\alias{lgcp_IC}
\alias{delta_IC}
\title{Information criteria for point processes}
\usage{
lgcp_IC(fit, data, predictor, domain, samplers, n.samples = 2000L)

delta_IC(data)
}
\arguments{
\item{fit}{A fitted \code{bru} model object, from \code{\link[=bru]{bru()}} or \code{\link[=lgcp]{lgcp()}}.}

\item{data}{A data frame containing the observed points, with columns for
coordinates and any covariates used in the model. The data frame may also
include a column named \code{.block} that indicates the block membership of each
point, if blockwise criteria are to be calculated.}

\item{predictor}{An expression that evaluates to the linear predictor of the
model, given the data. This should be an expression that can be evaluated
in the context of the data frame and model components, just as in the
\code{formula} argument of \code{\link[=bru_obs]{bru_obs()}} and \code{\link[=lgcp]{lgcp()}}.}

\item{domain, samplers}{Arguments defining the integration domain(s) and
sampling regions, as for \code{\link[=bru_obs]{bru_obs()}} and \code{\link[=lgcp]{lgcp()}}.}

\item{n.samples}{Integer. The number of posterior samples to draw for
estimating the criteria. Default is 2000.}
}
\value{
A \code{tibble} with columns for the \code{Criterion} name, \code{Method},
log pointwise predictive density (\code{lppd}), effective number of parameters
(\code{p_eff}), and the information criterion value (\code{IC})
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Calculate information criteria for point process models, using a fitted
model and optionally new data. The interface is still under development
and may change without warning in future versions.
}
\section{Functions}{
\itemize{
\item \code{delta_IC()}: Calculate IC differences based on joined output from
several \code{lgcp_IC} calls;

\if{html}{\out{<div class="sourceCode">}}\preformatted{delta_IC(rbind(cbind(lgcp_IC(...), Model = "A"),
               cbind(lgcp_IC(...), Model = "B")))
}\if{html}{\out{</div>}}

}}
\keyword{internal}
