% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.model.R
\name{plot.spatial}
\alias{plot}
\alias{plot.spatial}
\title{Plot spatial field}
\usage{
plot.spatial(model = NULL, result = NULL, data = NULL, effect = NULL,
  property = "mean", group = NULL, mesh = NULL, mask = NULL,
  col = NULL, add.mesh = TRUE, add.detection = FALSE,
  add.segment = FALSE, logscale = TRUE, rgl = FALSE, add = FALSE,
  nx = 300)
}
\arguments{
\item{model}{A \link{model} object}

\item{result}{An \link{inla} object, i.e. the result of running INLA}

\item{data}{A \link{dsdata} object}

\item{effect}{Needed if no model or col is provided. This selects the random field to plot, e.g. "spde". For predictions this corresponds to the tag parameter.}

\item{property}{Which property of the spatial field to plot, e.g. 'mode' (default)}

\item{group}{A data frame that is merged with the spatial locations at which we plot the data. For instance, in temporal SPDE models, we can set group = data.frame(group=1:10).}

\item{mesh}{The mesh used for spatial plotting. If given, overrides meshes from data and model.}

\item{mask}{A spatial mask restricting the region where we plot. A mask can be one of the following objects:
(1) A data frame with x and y coordinated defining a polygon. (2) An inla.mesh.segment object. (3) A SpatialPolygonDataFrame (see sp package)}

\item{col}{A numeric vector of values to plot. It's length must be equal to the number of mesh nodes. Overrides all data coming from the inla result.}

\item{add.mesh}{If TRUE, plot the mesh}

\item{add.detection}{If TRUE, plot the detections (requires: data)}

\item{add.segment}{If TRUE, plot the segments (requires: data)}

\item{logscale}{If set to FALSE, exp() of the values is plotted}

\item{rgl}{Use rgl to plot}

\item{add}{Add to previous rgl plot}
}
\value{
gg A ggplot2 object (if rgl=FALSE)
}
\description{
Plots spatial \link{model}s given an \link{inla} result object.
}
\details{
The main purpose of this function is to visualize spatial model posteriors using the parameters
\code{model}, \code{result} and \code{data}. The model is then evaluated on the data set's mesh
and using the \code{property} setting. The latter determins which property of the latent effects
are used of each effect to be combined. 
Options are: 'mode', 'mean', 'sd', '0.975quant', '0.025quant', 'kld'. Default is 'mean'.

The other paramters of this function mainly serve a purpose if one of the three main paramters
is not set. The \code{result} object is not needed if the \code{col} parameter is provided as
the latter already defines the spatial field to be plotted. Moreover, if no \code{model} is
provided, the \code{name} parameter is used to select an effect from the INLA \code{result}.
}

