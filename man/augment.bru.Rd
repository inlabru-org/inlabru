% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidiers.R
\name{augment.bru}
\alias{augment.bru}
\title{Augment a bru model fit with fitted values}
\usage{
\method{augment}{bru}(x, data, pred_formula, n_samples = 500L, seed = NULL, ...)
}
\arguments{
\item{x}{A fitted \code{bru} object.}

\item{data}{A data frame of covariate values.}

\item{pred_formula}{A formula passed to \code{inlabru::predict()}.}

\item{n_samples}{Number of posterior samples (default 500).}

\item{seed}{Optional integer. If non-\code{NULL}, the session RNG is reset to
this value before \code{inlabru::predict()} is called so that two identical
calls return identical posterior summaries. \code{NULL} leaves the session
RNG state unchanged (predictions then vary run-to-run because
\code{inlabru::predict()} samples from the posterior).}

\item{...}{Passed to \code{inlabru::predict()}.}
}
\value{
\code{data} with additional columns \code{.fitted}, \code{.fitted_low},
\code{.fitted_high}, \code{.fitted_sd}.
}
\description{
Adds posterior mean and credible interval columns to \code{data}. The
user must supply \code{pred_formula} because inlabru prediction expressions are
arbitrary R and cannot be recovered from the fit object alone. Also see
\code{\link[generics:augment]{generics::augment()}}.
}
