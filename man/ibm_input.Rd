% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bru_input.R
\name{ibm_input}
\alias{ibm_input}
\alias{ibm_input_set}
\alias{ibm_input_create}
\alias{ibm_input_available}
\alias{ibm_input_get}
\alias{bru_input.bru_mapper}
\alias{bru_input.bm_pipe}
\alias{bru_input.bm_multi}
\alias{bru_input.bm_collect}
\alias{bru_input.bm_repeat}
\alias{bru_input.bm_sum}
\alias{bm_autodetect}
\title{Interface between \code{bru_input} and \code{bru_mapper}}
\usage{
ibm_input_set(mapper, input)

ibm_input_create(mapper, ...)

ibm_input_available(mapper)

ibm_input_get(mapper)

\method{bru_input}{bru_mapper}(input, ..., label = "<unknown>")

\method{bru_input}{bm_pipe}(input, ..., label = "<unknown>")

\method{bru_input}{bm_multi}(input, ..., label = "<unknown>")

\method{bru_input}{bm_collect}(input, ..., label = "<unknown>")

\method{bru_input}{bm_repeat}(input, ..., label = "<unknown>")

\method{bru_input}{bm_sum}(input, ..., label = "<unknown>")

bm_autodetect()
}
\arguments{
\item{input}{A \code{bru_input} object, or \code{NULL} to remove any existing input.
Alternatively, an existing \code{bru_mapper} object with or without associated
input can be provided, in which case the input from that mapper
is copied.}
}
\description{
Associate \link{bru_input} objects with \link{bru_mapper} objects.
}
\section{Functions}{
\itemize{
\item \code{ibm_input_set()}: Add an existing \code{bru_input} to a \code{bru_mapper}.

\item \code{ibm_input_create()}: Create and add a \code{bru_input} to a \code{bru_mapper}.

\item \code{ibm_input_available()}: Check if a \code{bru_input} is associated with a
\code{bru_mapper}.

\item \code{ibm_input_get()}: Get the \code{bru_input} associated with a \code{bru_mapper}.

\item \code{bru_input(bru_mapper)}: Evaluate the input associated with a \code{bru_mapper}.

\item \code{bru_input(bm_pipe)}: Evaluate the inputs for each sub-mapper in a \code{bm_pipe}
object.

\item \code{bru_input(bm_multi)}: Evaluate the inputs for each sub-mapper in a \code{bm_multi}
object.

\item \code{bru_input(bm_collect)}: Evaluate the inputs for each sub-mapper in a
\code{bm_collect} object.

\item \code{bru_input(bm_repeat)}: Evaluate the inputs for the sub-mapper in a \code{bm_repeat}
object.

\item \code{bru_input(bm_sum)}: Evaluate the inputs for each sub-mapper in a \code{bm_sum}
object.

\item \code{bm_autodetect()}: Create a \code{bru_mapper} placeholder object of class
\code{bm_autodetect}. The main purpose of this class is to attach \link{bru_input}
information to it, which is later used to determine a suitable 'real'
mapper type.

}}
\examples{
(m <- bm_autodetect())
ibm_input_available(m)
(m <- ibm_input_create(m, cos(x)))
ibm_input_available(m)
ibm_input_set(bm_linear(), m)

}
