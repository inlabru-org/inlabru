% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{model.spde}
\alias{model.spde}
\title{Spatial SPDE model}
\usage{
model.spde(data, effect = "spde", n.group = 1, group = data$time.coords,
  covariate = NULL, ...)
}
\arguments{
\item{data}{Data set to read the names of the mesh coordinates from (mesh.coords)}

\item{effect}{A string defining the name of the SPDE effect}

\item{n.group}{Number of SPDE model groups (see \link{f}), e.g. for temporal models.}

\item{group}{A string defining the data column to group by, e.g. "season". Groups must be integer and > 0}

\item{covariate}{A function that maps a data frame of points to a scaling factor for the SPDE at that point.}

\item{...}{Arguments passed on to inla.spde2.matern. If none, the defaults are alpha = 2, prior.variance.nominal = 10, theta.prior.prec = 0.01}
}
\description{
Constructs a spatial SPDE model
}
\details{
This model uses the formula

~ . + f(spde, model = spde.mdl, group = spde.group, ...)
 
 where the name 'spde' can be replaced by the \code{effect} parameter. The internal INLA spde model
 spde.mdl is constructed using the dotted arguments. If these do not contain a mesh, the mesh of the
 data set \code{data} is used. If there are no ... arguments, it sets alpha = 2, 
 prior.variance.nominal = 10 and theta.prior.prec = 0.01.
}

